//Task 24 : Declare a nullable  String? nickname. Print it — but if it's null, print 'No
// nickname' using ??.
// Hint: String? nickname;  print(nickname ?? 'No nickname');  — ?? returns the right side if left is null
void main(){
  String? nickname;
  print(nickname ?? 'No nickname');

}